fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
yfinance>=0.2.40
xgboost>=2.0
lightgbm>=4.3
shap>=0.45
scikit-learn==1.7.2
# Pinned (not >=) to the locally-tested versions: these were open lower bounds
# and drift-prone between local and deploy. pandas-ta is a beta package
# historically brittle against newer numpy/pandas, so numpy is pinned explicitly
# (it is otherwise transitive) to lock the whole pandas/numpy/pandas-ta trio.
numpy==2.2.6
pandas==2.3.2
pandas-ta==0.4.71b0
python-dotenv>=1.0
joblib>=1.3
langgraph>=0.2,<1.0
groq>=0.11
transformers>=4.40
torch==2.12.1
requests>=2.31
chromadb>=0.5
sentence-transformers>=2.7
langchain-text-splitters>=0.2
beautifulsoup4>=4.12
ftfy>=6.2
mlflow==3.14.0
evidently>=0.4
matplotlib>=3.7
sqlalchemy>=2.0
passlib[bcrypt]>=1.7
# passlib 1.7.4's bcrypt backend breaks on bcrypt>=4.1 (raises on its 72-byte
# self-probe); pin to the last compatible bcrypt until passlib is updated.
bcrypt>=4.0,<4.1
PyJWT>=2.8
email-validator>=2.0

# --- dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
