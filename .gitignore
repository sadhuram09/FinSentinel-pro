.env
venv/
__pycache__/
node_modules/
*.pyc
.pytest_cache/
chroma_db/
mlruns/

# Trained model artifacts — too large and not reproducible from git;
# regenerate with `python scripts/train_models.py`.
# Leading slash anchors to the project root so it does NOT also match the
# backend/ml/models/ source package.
/models/

# Persisted RAG vector store (ChromaDB) — large, regenerable from SEC filings.
/rag_store/

# MLOps local artifacts — all regenerable, none belong in git.
# (mlruns/ is already ignored above for MLflow artifacts.)
/mlartifacts/
mlflow.db
*.sqlite
*.db
/drift_reports/